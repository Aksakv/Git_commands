git config --global user.name "aksa">> config git with our username

git config --global user.email "aksakuruvilla@gmail.com" >> config git with our emailid

configuration requires while pushing towards a public repository in github from aglobal repository

git status >> used to check the status of .git in the current directory

git init >> to initalize  git in the  current  directory (helps t track the changes in our directory 
* if we add, delete a file it will track it as a change in the directoty ),

U >> Denoted untracked  (if is there not yet commited or not added to staging area)

A >> Denoted  added  to stagging area
 
 M >> file being modified  after committed.

 git add into .txt >> intro.txt added to staging area 

 git add. >> all the files are added to the staging area

 A >> file that are added to stagging area
 
 git commit  -m " my first  committ "

 git log  >>  use to check the records of commit .
 
 git clone [repository link] >>  for clone a repository from github .
 
 git new branch .
 
